---
processes:
- name: redis
  executable: /var/vcap/packages/redis/bin/redis-server
  args:
    - /var/vcap/jobs/redis/config/redis.conf
  ephemeral_disk: true
  persistent_disk: true
  limits:
    memory: 1G
    open_files: 1024
  hooks:
    pre_start: /var/vcap/jobs/redis/bin/bpm-pre-start
